// Specify resources for processes based on labels
process {
    // Single-core processes
    withLabel: medium {
        cpus = 2
        memory = 4.GB
    }
    withLabel: small {
        cpus = 1
        memory = 2.GB
    }
}
